# Hotkey and minor features

Here is a list of minor features and tmux-specific keybindings that would improve our UX:

- Hotkey => Rename current bee (maybe cmd+r)
- Hotkey => Rename current workspace (maybe cmd+shift+r)
- Hotkey => Spawn swarm from frame, select from template, UI selector in tmux
- Hotkey => Spawn swarm from flow, UI selector in tmux
- Hotkey => 
- Parent association between bees (owned-by or reports-to). Gives easy filter for bees to sort on the bees they 'own'.
- Better selector UX (cmd+s), showing workspaces, quests, and bees grouped by colony and then swarm.
- Hotkey => List all website URLs posted in bee session + fzf + hotkey for opening in default browser.
- Automatic functionality for archiving and storing all bee sessions, for later indexation and usage.
- *Must probably live in the Syn project*: Deep integration with "Syn". Hotkey for spinning up an agent to accept the packet immediately after it is completed, another hotkey to spin up an agent fixing **everything so far, since the last checkpoint (time this hotkey was used)**. Must integrate with hive colonies, workspaces, and quests, and allow setting this context before starting.
- Hotkey => Move bee to colony
