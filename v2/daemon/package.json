{
  "name": "@honeybee/daemon-v2",
  "private": true,
  "version": "0.0.1",
  "description": "Honeybee reset WP4: the real daemon (loops, RPC surface, service layer, I1 telemetry). Spec: docs/design/specs/reset-04-daemon.md",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "test": "node --test \"tests/*.test.ts\""
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "25.7.0",
    "typescript": "6.0.3"
  }
}
